import React, { FC } from 'react';
import { Page } from '../components/Page';

export const ContactPage: FC = () => {
    return (
        <Page>
            <h1>Contact</h1>
        </Page>
    );
};
